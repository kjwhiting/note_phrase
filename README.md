# note_phrase
Convert a given phrase or word to a series of notes that can be played on a guitar.

```pip Install midiutil```

To get started run the main.py file

```python -m main```

Enter the phrase. The program will remove whitespace and only look at lowercase characters [a-z]. It will provide a `phase.mid` file that can be imported into DAW.
